import { createContext } from 'react';

/**
 * Contexto para el estado de búsqueda
 * Permite compartir el término de búsqueda entre componentes
 */
export const SearchContext = createContext(undefined);
